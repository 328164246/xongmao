<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.myapplication">

    <uses-permission android:name="android.permission.WRITE_APN_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.Homepage"
            android:launchMode="singleTask" />
        <activity android:name=".view.StartPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.Person_yuanchuang" />
        <activity android:name=".view.Lishi" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.Person_set" />
        <activity android:name=".view.Person_login" />
        <activity android:name=".view.Person_shoucang" />
        <activity android:name=".view.ClickVideoActivity" />
        <activity android:name=".view.Person_find" />
        <activity android:name=".view.Person_zhuceActivity"></activity>
    </application>

</manifest>